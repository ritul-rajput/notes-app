# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration (Optional - uses in-memory if not set)
MONGODB_URI=mongodb://localhost:27017/quick-notes

# Firebase Admin SDK (Optional - for auth verification)
# Download your Firebase Admin SDK JSON and set path here
FIREBASE_ADMIN_SDK_PATH=./firebase-admin-sdk.json

# CORS Settings
ALLOWED_ORIGINS=http://localhost:8081,exp://192.168.1.100:8081
